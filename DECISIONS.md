# سجل القرارات - Daleely.ai

**آخر تحديث:** اليوم

---

## 📋 Decision Log

| ID | القرار | السبب | التاريخ | الأثر |
|---|---|---|---|---|
| D001 | استخدام Next.js 14 App Router | أحدث إصدار، Server Components افتراضياً، SEO أفضل | اليوم | جميع الصفحات Server Components ما أمكن |
| D002 | TypeScript صارم | Type safety، سهولة الصيانة | اليوم | جميع الملفات TypeScript |
| D003 | Tailwind CSS فقط | سرعة التطوير، consistency | اليوم | لا CSS مخصص إلا في globals.css |
| D004 | Web Speech API مؤقت | سريع التنفيذ، لا يحتاج ملفات | اليوم | جاهز للاستبدال بملفات صوتية لاحقاً |
| D005 | بيانات في TypeScript arrays | بسيط، لا يحتاج قاعدة بيانات الآن | اليوم | يمكن تحويلها لـ API/DB لاحقاً |
| D006 | RTL فقط | المشروع عربي بالكامل | اليوم | لا دعم LTR حالياً |
| D007 | لا تسجيل دخول | سهولة الاستخدام، خصوصية | اليوم | جميع الأدوات متاحة بدون حساب |
| D008 | قوالب موحدة (Tool/Pillar/Article) | تقليل التكرار، consistency | اليوم | كل صفحة تستخدم قالبها المناسب |
| D009 | نظام نطق موحد (SpeechProvider) | مرونة، قابل للاستبدال | اليوم | يمكن تبديل Web Speech API بملفات صوتية |
| D010 | SEO كامل لكل صفحة | تحسين البحث، Schema markup | اليوم | كل صفحة لها metadata + Schema |
| D011 | صفحات FAQ منفصلة | SEO أفضل، محتوى غزير لكل سؤال | اليوم | كل سؤال FAQ له صفحة مقال منفصلة |
| D012 | محتوى أولياء الأمور | زيادة القيمة، SEO، تكرار الاستخدام | اليوم | مكتبة موارد كاملة + خطط 14 يوم + FAQ |
| D013 | محتوى المعلمين | زيادة القيمة، SEO، تكرار الاستخدام | اليوم | مكتبة موارد + نوتات جاهزة + مقالات |

---

## 🎯 معايير التوسع

### 1. اللغات (i18n)

**القرار:** دعم RTL فقط حالياً (العربية)  
**التوسع المستقبلي:**
- استخدام Next.js i18n routing: `app/[locale]/...`
- هيكل: `app/ar/...` و `app/en/...` (إذا أضفنا الإنجليزية)
- ملفات الترجمة: `lib/i18n/[locale].json`
- **قاعدة:** كل لغة لها مجلد منفصل في `app/`

**قواعد التسمية:**
- المجلدات: `[locale]` (ar, en, fr, etc.)
- الملفات: نفس الأسماء، المحتوى يختلف
- البيانات: `lib/tools/[locale].ts` أو `lib/tools.ts` مع locale field

---

### 2. RTL/LTR

**القرار الحالي:** RTL فقط (العربية)  
**التوسع:**
- استخدام `dir` attribute ديناميكي: `<html dir={locale === 'ar' ? 'rtl' : 'ltr'}>`
- Tailwind RTL: استخدام `rtl:` prefix عند الحاجة
- **قاعدة:** كل locale يحدد اتجاهه

---

### 3. بنية المحتوى

**القرار الحالي:** 
- الأدوات: `lib/tools.ts` (array)
- المقالات: `lib/articles.ts` (array)

**التوسع:**
- إضافة `locale` field لكل tool/article
- أو: `lib/tools/[locale].ts` (ملفات منفصلة)
- أو: قاعدة بيانات مع locale column
- **قاعدة:** البيانات منفصلة حسب اللغة، أو مع locale field

---

### 4. SEO

**القرار:** SEO كامل لكل صفحة  
**التوسع:**
- `metadataBase` يتغير حسب locale: `new URL("https://daleely.ai/ar")`
- `hreflang` tags للصفحات متعددة اللغات
- Schema markup مع `inLanguage` field
- **قاعدة:** كل locale له canonical URL منفصل

---

### 5. النطق/الصوت

**القرار الحالي:** Web Speech API  
**التوسع:**
- استبدال `lib/speech/web-speech.ts` بـ `lib/speech/audio-files.ts`
- ملفات صوتية: `public/sounds/[locale]/[type]/[file].mp3`
- هيكل: `public/sounds/ar/numbers/1.mp3`, `public/sounds/en/numbers/1.mp3`
- **قاعدة:** الواجهة موحدة (SpeechProvider)، التنفيذ قابل للتبديل

---

### 6. المواد التعليمية الجديدة

**القرار الحالي:** math و arabic فقط  
**التوسع:**
- إضافة `subject` field: `category: "math" | "arabic" | "science" | "english"`
- أو: `lib/tools/[subject].ts`
- Pillars جديدة: `app/[subject]/page.tsx`
- **قاعدة:** كل مادة لها Pillar منفصل + أدواتها

---

### 7. الصفوف الدراسية

**القرار الحالي:** "1-2" | "3-4" | "5-6" | "all"  
**التوسع:**
- إضافة صفوف جديدة: "7-8" | "9-10" | etc.
- أو: نظام مرن `gradeLevels: number[]`
- **قاعدة:** `gradeLevel` قابل للتوسع، لا hardcode

---

### 8. أنواع الأدوات

**القرار الحالي:** Interactive tools فقط  
**التوسع:**
- إضافة `type` field: `"interactive" | "worksheet" | "video" | "game"`
- أو: `lib/tools/[type]/...`
- **قاعدة:** نوع الأداة يحدد القالب المستخدم

---

## 🔒 قواعد غير قابلة للنقاش

1. **لا LTR في الواجهة العربية:** RTL فقط للعربية
2. **لا تسجيل دخول إجباري:** جميع الأدوات متاحة بدون حساب
3. **نطق حقيقي:** الأعداد/الحروف/الكلمات تُنطق، ليس فقط أصوات تنبيه
4. **SEO كامل:** كل صفحة لها metadata + Schema
5. **TypeScript صارم:** لا JavaScript عادي
6. **Server Components أولاً:** Client Components فقط عند الحاجة
7. **قوالب موحدة:** لا تكرار في الكود
8. **Privacy-first:** لا جمع بيانات شخصية بدون موافقة

---

## 📝 ملاحظات التوسع

### عند إضافة لغة جديدة:
1. إنشاء `app/[locale]/...` structure
2. إضافة ملفات الترجمة `lib/i18n/[locale].json`
3. تحديث `metadataBase` في layout
4. إضافة `hreflang` tags
5. تحديث sitemap ليشمل جميع اللغات

### عند إضافة مادة جديدة:
1. إنشاء `app/[subject]/page.tsx` (Pillar)
2. إضافة أدوات في `lib/tools.ts` مع `category: "subject"`
3. إضافة مقالات في `lib/articles.ts`
4. تحديث Navigation في Header

### عند إضافة نوع أداة جديد:
1. تحديد القالب المناسب (ToolTemplate أو قالب جديد)
2. إنشاء مكونات الأداة في `components/tools/`
3. إضافة صفحة في `app/tools/[slug]/page.tsx`
4. إضافة بيانات في `lib/tools.ts`

---

**آخر تحديث:** اليوم
